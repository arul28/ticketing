<!DOCTYPE html>
<html>
  <head>
    <title>RailTicketing</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <header>
      <nav>
      </nav>
    </header>

    <main>
      <%= yield %>
    </main>
    <% if request.path != root_path && request.path != admin_dashboard_index_path && request.path != passenger_dashboard_index_path %>
      <%= link_to "Back to Home Page", root_path, class: 'btn btn-primary' %>
    <% end %>

  </body>
</html>

