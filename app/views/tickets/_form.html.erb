<%= form_with(model: ticket) do |form| %>
  <% if ticket.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
        <% ticket.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    Departure Station: <%= params[:departure_station]%>
  </div>
  <div>
    Termination Station: <%= params[:termination_station]%>
  </div>
  <div>
    Departure Date: <%= params[:departure_date]%>
  </div>
  <div>
    Ticket Price: <%= params[:ticket_price]%>
  </div>
  <div>
    <%= form.label :passenger_id, hidden: true %>
    <%= form.number_field :passenger_id, value: params[:passenger_id], hidden: true %>
  </div>
  <div>
    <%= form.label :train_id, hidden: true %>
    <%= form.number_field :train_id, value: params[:train_id], hidden: true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
